<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VascularSurgeryProject" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Primary colors -->
        <item name="colorPrimary">@color/botticelli</item>
        <item name="colorPrimaryVariant">@color/rockBlue</item>
        <item name="colorOnPrimary">@color/veniceBlue</item>

        <!-- Background and surface -->
        <item name="backgroundColor">@color/catskillWhite</item>
        <item name="colorSurface">@color/jungleMist</item>

        <!-- Text colors -->
        <item name="colorOnBackground">@color/black</item>
        <item name="colorOnSurface">@color/black</item>

        <!-- Other custom colors -->
        <item name="colorAccent">@color/monzaRed</item>
        <item name="colorError">@color/monzaRed</item>
    </style>
    <style name="Theme.VascularSurgeryProject" parent="Base.Theme.VascularSurgeryProject" />
</resources>